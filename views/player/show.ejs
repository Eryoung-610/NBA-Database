<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
</head>

<body>

    <div class="container center-align">
        <div class="left-align">
            <a href="/player"><i class="black-text material-icons">arrow_back</i></a>
        </div>

        <img src="https://en.as.com/img/comunes/fotos/fichas/deportistas/silueta-generica-large.png" alt="">
        <div>
            <h3><%= playerDetails.first_name+ " "+ playerDetails.last_name %></h3>
        </div>

        <br>
        <div>
            <h4>Statistics</h4>
            <hr>
        </div>

        <div class="left-align center-align">
            <% if (playerStats.data.length >= 1) { %>
                <h5>Games Played : <%= playerStats.data[0].games_played%></h5>
                <h5>Minutes Per Game: <%= playerStats.data[0].min%></h5>
                <h5>Points Per Game: <%= playerStats.data[0].pts%></h5>
                <h5>Rebounds Per Game: <%= playerStats.data[0].reb%></h5>
                <h5>Assists Per Game: <%= playerStats.data[0].ast%></h5>
                <h5>Blocks Per Game: <%= playerStats.data[0].blk%></h5>
                <h5>Steals Per Game: <%= playerStats.data[0].stl%></h5>
                <h5>3's Per Game: <%= playerStats.data[0].fg3m%></h5>
                
            <% } else { %>
                <h5>No current stats for this season</h5>
            <% } %>

        </div>

    </div>

</body>

</html>